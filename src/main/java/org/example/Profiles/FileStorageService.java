package org.example.Profiles;

import org.springframework.stereotype.Service;
import org.springframework.web.multipart.MultipartFile;

import java.nio.file.Files;
/*
@Service
public class FileStorageService {
    private static final String IMAGE_DIRECTORY = "Airline/src/main/resources/static/";
    private static final String IMAGE_URL = "http://localhost:8080/uploads/images";

    public String imageUpload (MultipartFile file){
        if(file.isEmpty()){
            throw new RuntimeException("Sorry the file is empty");
        }

        //Files.write(path, bytes);
    }
}*/
